services:
  db:
    image: mysql:8.0
    container_name: all_animals_db
    restart: always
    environment:
      MYSQL_DATABASE: all_animals
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data: